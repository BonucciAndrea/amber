# build artifacts (generated by build.sh / ./a — never commit these)
/o/
/amber
/k
*.o
*.so
*.dylib
*.a
libk.*
.ltocheck

# out-of-tree extensions install themselves here (see ext/README.md);
# they belong to their own repositories, never to this one.
/ext/*.c
/ext/*.h
/lib/*.k

# scratch / local backups
_to_delete/
wow.txt

# Windows download cruft
*Zone.Identifier

# macOS Finder cruft
.DS_Store
