/ 10,000,000 element vector sum. Same idiom Amber's own bench.k uses; this
/ file's k-dialect (ngn/k-derived) is also what Amber itself accepts, so the
/ harness runs this exact file for both the `amber` and `k` (ngn/k) rows.
+/!10000000
